import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#FF045F",
    secondary: "#FFC100",
    tertiary: "#1E18CF",
  },
};

export default theme;
